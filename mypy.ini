[mypy]
# ソースコードのルートを指定（srcディレクトリを認識させるため）
mypy_path = opt/auto-wiki

# 外部ライブラリ（mwclient, schedule等）の型定義がない場合のエラーを無視
ignore_missing_imports = True

# チェック対象から除外するディレクトリ（MediaWikiのソースコード等）
exclude = opt/auto-wiki/data/

# 任意の型のキャストなどを許可（既存コードへの影響を最小限にするため）
no_strict_optional = False